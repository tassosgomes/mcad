{
  "name": "mcad-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "mcad monorepo — tooling root (commitlint + husky). Service builds use their own package.json/csproj/pom.xml.",
  "scripts": {
    "commitlint": "commitlint --from=HEAD~1 --to=HEAD",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.0",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7"
  }
}
