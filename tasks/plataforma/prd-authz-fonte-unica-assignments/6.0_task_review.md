# Validacao da Tarefa 6.0

## Resultado da Validacao Automatizada

APROVADA.

Comandos executados:

- `rtk npm test` em `frontend`: 21 arquivos de teste passaram, 80 testes passaram.
- `rtk npm run build` em `frontend`: `tsc -b && vite build` passou. Observacao: Vite emitiu o aviso existente de que `/runtime-env.js` em `index.html` nao pode ser empacotado sem `type="module"`.
- `rtk npm run build` em `services/bff`: `tsc -p tsconfig.json` passou.
- `rtk npm test` em `services/bff`: `node --test dist/*.test.js` passou, 4 suites/arquivos passaram.

Nao ha script `lint` ou `type-check` separado no `frontend/package.json`; o typecheck foi coberto pelo `npm run build`.

## Revisao Estatica Executada

Buscas executadas nas areas alteradas:

- `user.profile.roles`, `profile.roles`, `AuthContext.roles`, `hasRole(` e `x-mcad-roles`: nenhum uso funcional em codigo de producao das areas alvo.
- `apiAuthzClient`, `authzGet`, `authzPost`, `authzDelete`, `authzPut`, `AUTHZ_API_BASE_URL`, `/v1/users` e `/v1/roles` em `frontend/src/features/autorizacao`: nenhum acesso direto ao `ecad-authz`; a feature usa BFF.
- scope/claim `roles` em `frontend/src/shared/auth`: nenhum uso encontrado.

## Revisao Tecnica

APROVADA.

Evidencias revisadas:

- Callback pos-login resolve landing page por permissoes efetivas vindas de `/api/me/permissions`, sem usar roles do perfil OIDC (`frontend/src/shared/auth/CallbackPage.tsx`).
- `AuthContext` nao expoe `roles`; o provider mantem apenas usuario OIDC, estado de autenticacao e token para chamadas autenticadas.
- `authConfig` nao solicita scope/claim `roles`; o comentario deixa `access` e `write` como compatibilidade de audience, nao como autorizacao de negocio.
- Header consome `/api/me` via `useEffectiveProfile`, exibindo `primaryRole` retornado pelo BFF.
- BFF `/api/me` retorna roles efetivas e `primaryRole` a partir do contexto do `ecad-authz`; `/api/me/permissions` propaga `x-authz-version`.
- Tela de Atribuicoes usa BFF para assignments, busca de usuarios, papeis, atribuicao, remocao e historico.
- Mutações de atribuir/remover invalidam queries de assignments, historico e permissoes efetivas.
- Rotas e acoes sensiveis usam `RequirePermission`, `Can` e `usePermissions`, nao roles do token.
- Controles principais da tela possuem labels/nomes acessiveis, estados de loading/erro, feedback com `role="status"` e confirmacao explicita de remocao com usuario, papel e impacto esperado.
- Testes RTL cobrem callback por permissao efetiva, atribuicao, remocao com confirmacao, historico e ocultacao de acoes para acesso somente leitura.

## Problemas Encontrados

Nenhum problema bloqueante encontrado.

## Recomendacao Final

APROVADA.
