# Validacao da Tarefa 4.0

## Resultado Automatizado

APROVADA

Comandos executados:

- `rtk bash -n scripts/provision-logto.sh` - passou.
- `rtk bash -n scripts/seed-authz.sh` - passou.
- `rtk bash scripts/provision-logto.sh --check-no-business-roles` - passou; confirmou ausencia de chamadas ativas para provisionar roles/user roles ou criar/atualizar JWT customizer.
- `rtk jq empty seeds/mcad/assignments.json` - passou.
- `rtk jq -e 'all(.[]; (.email|type=="string") and (.name|type=="string") and (.subjectHint|type=="string") and (.roleKeys|type=="array"))' seeds/mcad/assignments.json` - passou.
- `rtk jq -e --slurpfile roles seeds/mcad/roles.json '[.[].roleKeys[]] as $assigned | ($roles[0] | map(.key)) as $known | all($assigned[]; $known | index(.))' seeds/mcad/assignments.json` - passou.
- `rtk jq -e 'map(select((.roleKeys|length)==0)) | length >= 1' seeds/mcad/assignments.json` - passou.
- `rtk jq -e '([.[].email] | length) == ([.[].email] | unique | length)' seeds/mcad/assignments.json` - passou.
- `rtk bash scripts/seed-authz.sh --dry-run` - passou; percorreu catalogos, roles e assignments sem chamadas reais.
- `rtk bash scripts/provision-logto.sh --help` - passou.
- `rtk bash scripts/seed-authz.sh --help` - passou.
- `rtk rg ... scripts/provision-logto.sh ...` - passou na revisao estatica; os unicos matches de `/roles`, `/users/.../roles` e `jwt-customizer` no provisionamento sao validadores/guardas e o `DELETE /configs/jwt-customizer/access-token` de limpeza.

Observacao: `shellcheck` nao esta instalado neste ambiente; por isso a validacao estatica de shell ficou limitada a `bash -n`, dry-run e revisao por busca.

## Resultado do Review Tecnico

APROVADO

Evidencias:

- `scripts/provision-logto.sh` agora provisiona apenas API Resource/audience, SPA e usuarios de teste.
- A lista de usuarios de teste nao contem campo `role`, `roleId` ou equivalente.
- Nao ha chamada ativa para Logto `/roles` nem `/users/{id}/roles` no comportamento de provisionamento.
- A criacao/atualizacao do customizer de access token com claim `roles` foi removida; o script apenas remove o customizer legado por `DELETE /configs/jwt-customizer/access-token`.
- O scope `write` nao permanece no provisionamento do API Resource; o script garante apenas o scope tecnico `access`.
- `seeds/mcad/assignments.json` contem fixtures explicitas para os usuarios Logto e usuarios sem papel com `roleKeys: []`.
- `scripts/seed-authz.sh --dry-run` processa assignments no `ecad-authz`, nao no Logto.
- `README.md` e `.env.example` documentam que Logto nao e fonte de assignments e apontam `scripts/seed-authz.sh` como fluxo DEV/CI.

Limitacao de ambiente:

- Nao foi executado login real nem validacao de token/audience contra tenant Logto, por ausencia de credenciais e por esta etapa solicitar checks viaveis sem credenciais. A cobertura local validou sintaxe, fixtures, dry-run e ausencia estatica/runtime de chamadas proibidas no script.

## Issues Encontradas

Nenhuma.

## Recomendacao Final

APROVADA
