# Task 1.0 Review - Remover autoassignment por roles do IdP no ecad-authz

## Resultado da Validacao Automatizada

APROVADA.

Comando executado:

```bash
rtk mvn -B -ntp test
```

Diretorio: `/home/tsgomes/github-tassosgomes/ecad-authz/backend`

Resultado:

- Build Maven multi-modulo concluido com sucesso.
- Spotless `check` executado pela fase `validate` em todos os modulos e sem violacoes.
- Checkstyle `check` executado pela fase `validate` em todos os modulos e sem violacoes.
- Testes executados com sucesso em todos os modulos do reactor.
- Totais observados por modulo:
  - `authz-domain`: 28 testes, 0 falhas.
  - `authz-application`: 69 testes, 0 falhas.
  - `authz-api`: 12 testes, 0 falhas.
  - `authz-infra`: 76 testes, 0 falhas.
  - `authz-bootstrap`: 8 testes, 0 falhas.
  - `authz-spring-boot-starter`: 22 testes, 0 falhas.
  - `sample-pilot`: 3 testes, 0 falhas.

## Revisao Tecnica

APROVADA.

Evidencias revisadas:

- `SyncIdentityUserUseCase` nao depende mais de `RoleRepository` e nao chama `saveAssignment` ou `findActiveAssignment` no fluxo de sync.
- `SyncIdentityUserResult` agora retorna `assignedRoles=0` e `ignoredRoleKeys`.
- Eventos legados com `roleKeys` seguem aceitos; os valores sao normalizados apenas para contagem diagnostica.
- Roles ignoradas registram metrica `authz_identity_role_keys_ignored_total` e log com contagem, sem expor as role keys.
- Testes cobrem evento com `roleKeys`, usuario novo com role legada, usuario suspenso/deletado, roles duplicadas/em branco e ausencia de roles.
- Busca estatica confirmou que `saveAssignment`/`findActiveAssignment` permanecem somente nos use cases oficiais de assign/remove e nao no sync de identidade.
- README documenta que assignments entram apenas pelas APIs oficiais do `ecad-authz`.

## Conformidade com PRD/TechSpec/Task

- RF-01 atendido: sync de identidade faz upsert/status e ignora roles vindas do IdP.
- Criterio de compatibilidade atendido: eventos antigos com `roleKeys` continuam parseados.
- Criterio de deny seguro atendido para o sync: nenhuma atribuicao automatica e nenhuma persistencia em `user_roles` pelo use case de identidade.
- Observabilidade atendida por metrica e log estruturado com contagem.
- Sem feature flag para reativar comportamento legado.
- Sem alteracao em endpoints oficiais de assignment.

## Problemas Encontrados

Nenhum problema bloqueante ou nao bloqueante identificado.

## Recomendacao Final

APROVADA.
