# Task 2.0 Review - Publicar sync de identidade sem roles no identity-sync-api

## Resultado da Validacao Automatizada

APROVADA.

Comandos executados:

```bash
rtk npm run build
rtk npm test
```

Diretorio: `/home/tsgomes/mcad/services/identity-sync-api`

Resultado:

- Build TypeScript concluido com sucesso via `tsc -p tsconfig.json`.
- Testes Node executados com sucesso via `node --test dist/*.test.js`.
- Resultado observado: 1 arquivo de teste aprovado, 0 falhas, 0 cancelados, 0 skipped.
- Nao ha script separado de lint no `package.json`; o build TypeScript foi usado como typecheck aplicavel.

## Revisao Tecnica

APROVADA.

Evidencias revisadas:

- `LogtoManagementClient.listUsers()` pagina somente `GET /api/users` e nao enriquece usuarios com `GET /users/{id}/roles`.
- `LogtoManagementClient.getUser()` busca somente `GET /users/{id}`.
- `LogtoUser` nao declara `roles`.
- `IdentityUserSnapshot` contem `logtoUserId`, `username`, `displayName`, `email`, `avatarUrl`, `isSuspended` e `raw`, sem dependencia de roles.
- `buildIdentityEvent()` nao publica `roles` nem `roleKeys` no snapshot.
- Campos legados `roles` e `roleKeys` sao removidos de `user.raw` antes da publicacao.
- Entradas legadas com roles incrementam `identity_sync_roles_ignored_total` pela contagem de itens recebidos, sem expor os valores das roles.
- Sync manual `POST /sync/logto/users` continua usando `syncLogtoUsers()` e publicando eventos com status 202 quando autorizado.
- Teste cobre que o client Logto nao faz chamada terminada em `/roles`.
- Teste cobre que evento publicado nao contem roles de negocio e que o raw sanitizado nao preserva `roles`/`roleKeys`.
- README documenta que assignments pertencem ao `ecad-authz` e que o sync nao chama `GET /users/{id}/roles`.

## Conformidade com PRD/TechSpec/Task

- RF-01 atendido para o `identity-sync-api`: o servico transporta identidade/status e nao alimenta assignments a partir do IdP.
- Criterios da task atendidos: build passa, testes passam, chamada `/users/{id}/roles` removida, evento publicado sem roles de negocio, sync manual preservado e README atualizado.
- Compatibilidade atendida por ausencia de `roles` no payload, forma permitida pela task e pela techspec.
- Skill `roles-naming` foi considerado apenas como referencia geral; neste PRD a decisao especifica e remover roles de negocio do IdP e do evento de identidade.

## Problemas Encontrados

Nenhum problema bloqueante ou nao bloqueante identificado.

## Recomendacao Final

APROVADA.
