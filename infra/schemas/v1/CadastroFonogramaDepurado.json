{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "http://schema-registry/v1/CadastroFonogramaDepurado.json",
  "title": "CadastroFonogramaDepurado",
  "description": "Publicado apos depuracao de fonograma duplicado. Novo fonograma canonico criado em substituicao.",
  "type": "object",
  "required": ["specversion", "id", "source", "type", "subject", "time", "datacontenttype", "data"],
  "properties": {
    "specversion": { "const": "1.0" },
    "id": { "type": "string", "format": "uuid" },
    "source": { "const": "urn:cadastro-api" },
    "type": { "const": "cadastro.fonograma.depurado" },
    "subject": { "type": "string", "format": "uuid" },
    "time": { "type": "string", "format": "date-time" },
    "datacontenttype": { "const": "application/json" },
    "data": { "$ref": "#/$defs/data" }
  },
  "additionalProperties": true,
  "$defs": {
    "data": {
      "type": "object",
      "required": ["fonogramaId", "isrcOriginal", "novoFonogramaId", "obraId"],
      "properties": {
        "fonogramaId": { "type": "string", "format": "uuid" },
        "isrcOriginal": { "type": "string" },
        "novoFonogramaId": { "type": "string", "format": "uuid" },
        "obraId": { "type": "string", "format": "uuid" }
      },
      "additionalProperties": false
    }
  }
}
