{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "http://schema-registry/v1/CadastroObraDominioPublico.json",
  "title": "CadastroObraDominioPublico",
  "description": "Publicado quando o flag de dominio publico de uma obra e alterado.",
  "type": "object",
  "required": ["specversion", "id", "source", "type", "subject", "time", "datacontenttype", "data"],
  "properties": {
    "specversion": { "const": "1.0" },
    "id": { "type": "string", "format": "uuid" },
    "source": { "const": "urn:cadastro-api" },
    "type": { "const": "cadastro.obra.dominio-publico" },
    "subject": { "type": "string", "format": "uuid" },
    "time": { "type": "string", "format": "date-time" },
    "datacontenttype": { "const": "application/json" },
    "data": { "$ref": "#/$defs/data" }
  },
  "additionalProperties": true,
  "$defs": {
    "data": {
      "type": "object",
      "required": ["obraId", "titulo", "dominioPublico"],
      "properties": {
        "obraId": { "type": "string", "format": "uuid" },
        "titulo": { "type": "string" },
        "dominioPublico": { "type": "boolean" }
      },
      "additionalProperties": false
    }
  }
}
